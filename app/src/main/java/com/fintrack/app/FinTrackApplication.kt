package com.fintrack.app

import android.app.Application

/**
 * Точка входа приложения. Держит контейнер зависимостей (база, репозиторий, фабрики ViewModel).
 */
class FinTrackApplication : Application()
